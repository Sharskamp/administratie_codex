import { prisma } from '@/lib/prisma';import Link from 'next/link';
export default async function Page(){const rows=await prisma.offerte.findMany({include:{klant:true},orderBy:{nummer:'desc'}});return <div className='card'><h1>Offertes</h1><form action='/api/offertes' method='post' style={{display:'grid',gap:8,maxWidth:500}}><input name='klantId' placeholder='Klant ID'/><input name='omschrijving' placeholder='Omschrijving'/><input name='prijs' placeholder='Bedrag'/><button className='btn'>Offerte maken</button></form><table><thead><tr><th>Nummer</th><th>Klant</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.nummer}</td><td>{r.klant.naam}</td><td>{r.status}</td><td><Link className='btn' href={`/api/offertes?convert=${r.id}`}>Naar factuur</Link></td></tr>)}</tbody></table></div>}
