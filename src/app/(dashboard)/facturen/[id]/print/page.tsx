import { prisma } from '@/lib/prisma';import { euro,d } from '@/lib/utils';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const f=await prisma.factuur.findUnique({where:{id},include:{klant:true,regels:true}});if(!f) return <div className='card'>Niet gevonden</div>;return <div className='card'><h1>Factuur {f.nummer}</h1><p>{f.klant.naam}</p><p>Datum {d(f.datum)}</p><table><thead><tr><th>Omschrijving</th><th>Aantal</th><th>Prijs</th></tr></thead><tbody>{f.regels.map(r=><tr key={r.id}><td>{r.omschrijving}</td><td>{r.aantal}</td><td>{euro(r.prijs)}</td></tr>)}</tbody></table><h3>Totaal {euro(f.totaal)}</h3></div>}
